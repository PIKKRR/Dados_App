<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_dados"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_dados"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dados"
        tools:targetApi="31">

        <activity android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".OpcionesActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="uiMode|screenSize|orientation|keyboardHidden" />
        <activity android:name=".JuegoActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="uiMode|screenSize|orientation|keyboardHidden" />
        <activity android:name=".ResultadosActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="uiMode|screenSize|orientation|keyboardHidden" />
        <activity android:name=".ListaJugadoresActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="uiMode|screenSize|orientation|keyboardHidden" />
        <activity android:name=".Top15Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:configChanges="uiMode|screenSize|orientation|keyboardHidden" />
    </application>

</manifest>